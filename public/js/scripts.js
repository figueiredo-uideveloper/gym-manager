const buttons=Array.from(document.querySelectorAll(".dropdown-button"));for(button of buttons){toggleDataShow(button)}function toggleDataShow(o){o.addEventListener("click",(function(){let o=this.nextElementSibling;if(o.classList.contains("data-show"))o.classList.remove("data-show");else{let t=document.getElementsByClassName("dropdown");for(dropdown of t)dropdown.classList.contains("data-show")&&dropdown.classList.remove("data-show");o.classList.add("data-show")}}))}window.onclick=function(o){if(!o.target.matches(".dropdown-button")){let o=document.getElementsByClassName("dropdown");for(dropdown of o)dropdown.classList.contains("data-show")&&dropdown.classList.remove("data-show")}};
let deleteButtons=Array.from(document.querySelectorAll("[delete-button]")),modalCancelButton=document.querySelector("[modal-cancel-button]");for(deleteButton of(modalCancelButton.addEventListener("click",toggleModal),deleteButtons))deleteButton.addEventListener("click",toggleModal);function toggleModal(){document.querySelector(".modal-wrapper").classList.toggle("show")}